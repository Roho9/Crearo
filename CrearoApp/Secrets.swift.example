// Template for the gitignored CrearoApp/Secrets.swift.
// Copy this file to CrearoApp/Secrets.swift (drop the ".example") and paste your Anthropic API key.
// The real Secrets.swift is gitignored so the key is never committed. Leaving the key empty runs
// the game offline with the deterministic stub interpreter.
//
//   cp CrearoApp/Secrets.swift.example CrearoApp/Secrets.swift
//
import Foundation

enum Secrets {
    static let anthropicAPIKey = ""   // "sk-ant-..." for local Forge testing; empty = offline stub
}
